{
  "name": "indexprep-backend",
  "version": "0.1.0",
  "description": "IndexPrep learning platform — backend API + SQLite database (Phase 2a)",
  "type": "commonjs",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server/server.js"
  }
}
