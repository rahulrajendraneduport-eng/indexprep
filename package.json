{
  "name": "indexprep-backend",
  "version": "0.2.0",
  "description": "IndexPrep learning platform — backend API (SQLite locally, Postgres in production)",
  "type": "commonjs",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server/server.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
